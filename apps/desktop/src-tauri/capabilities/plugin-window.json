{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugin-window",
  "description": "Allow host-managed plugin windows to be dragged and closed",
  "windows": ["plugin-window-*"],
  "permissions": ["core:window:allow-start-dragging", "core:window:allow-close"]
}
