# @greywork/workbench

GreyWork 的共享工作台 UI 包：桌面端与 Web 端共用的完整界面（GreyWork Core、Agent 编排、GIS 地图、编辑器、数据洞察、插件市场）。

- 依赖领域包（agents / spatial / gis / analytics / plugins / shell）
- 由 `GreyWorkWorkbench` 单一入口导出
